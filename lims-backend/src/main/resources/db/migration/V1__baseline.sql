-- =============================================================
-- LIMS Database Schema — Baseline Migration
-- V1__baseline.sql
-- =============================================================
-- This migration creates the initial schema for the LIMS system.
-- All subsequent schema changes should be added as new migrations:
--   V2__description.sql, V3__description.sql, etc.
-- =============================================================

-- Flyway baseline marker — schema will be built incrementally.
-- The full schema migrations will be added in Phase 3.

SELECT 1;
